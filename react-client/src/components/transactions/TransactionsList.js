import React, { PropTypes } from 'react'

//{ categories, onInBudgetChange }
const TransactionsList = () => (
    <div>
        <h2>Transactions List</h2>
    </div>
)

TransactionsList.propTypes = {
}

export default TransactionsList
